# 設計仕様書

## OMR センサー仕様

### センサーの動作原理

- センサーの形状：横長
- 赤外線を照射し、反射でマークの塗りを判定
- 左端（縦型）または下端（横型）にタイミングマーク読み取り穴
- ピッチに合わせた間隔でマークボックス読み取り穴が並ぶ

### 対応機器

セコニック SR-3500（およびピッチ互換機）

---

## 用紙仕様

| 項目 | 値 |
|---|---|
| 印刷サイズ | A4 |
| アートボード | B4（裁ち落とし込み） |
| A4 配置 | B4 中央 |
| A4 Short | 210 mm |
| A4 Long | 297 mm |
| B4 Short | 257 mm |
| B4 Long | 364 mm |

---

## シート向きとセンサー通過方向

センサーに対してシートが通る方向は常に同じ。  
文字を読みやすく配置するために縦型・横型の2種類がある。

| 向き | センサー通過方向 | TM 配置辺 | MB 展開方向（紙面上） |
|---|---|---|---|
| 縦型 | 上から下 | 左辺 | 左→右 |
| 横型 | 左から右 | 下辺 | 下→上 |

---

## 座標系

### u/v 内部座標系

全ての座標計算を「センサー基準」の内部座標系で行う。

```
u 軸：センサー通過方向
  縦型 → 紙面の上→下（PDF の Y 方向）
  横型 → 紙面の左→右（PDF の X 方向）

v 軸：MB 展開方向
  縦型 → 紙面の左→右（PDF の X 方向）
  横型 → 紙面の下→上（v 原点が下端）
```

### u,v → 描画座標 (x,y) 変換

```
縦型：x = offV + v,  y = offU + u
横型：x = offU + u,  y = offV + (vLen - v)
```

---

## タイミングマーク（TM）仕様

### 基本仕様

- TM・MB の長辺方向はマークシートの長辺方向と**逆**
  - 縦型（シート長辺 = u 方向）→ TM・MB 長辺 = v 方向（横長）
  - 横型（シート長辺 = v 方向）→ TM・MB 長辺 = u 方向（縦長）

### TM v 座標（外枠端からのオフセット）

```
おもて面：TM v 開始 = outer.v1 + tmPos
うら面  ：TM v 開始 = outer.v2 - tmPos - tmV
```

tmPos が負値の場合、外枠より外側（塗り足し対応）。

### 制御型 TM の u 座標

```
TM u 開始 = MB 中心 + tmU × 1.5 - tmU / 2
```

### TM 最小間隔

制御型：**4.23 mm**

### TM 重複排除

受験番号欄と解答欄の TM u 座標を収集後、0.01 mm 以内を同一とみなし1本に統合。

---

## マークボックス（MB）仕様

### サイズ

デフォルト：短辺 2.5 mm × 長辺 4.0 mm（変更可）

### u/v 方向への割り当て

```
縦型：mbU = mbS（短辺），mbV = mbL（長辺）
横型：mbU = mbL（長辺），mbV = mbS（短辺）
```

### MB 識別（Illustrator スクリプト互換）

- strokeColor.magenta ≥ 50 → マークボックス
- fillColor.black ≥ 70 → ID マーク

### MB 内選択肢番号

- MB 枠と同色（マゼンタ）で表示
- MB 幅の 80% 以内に収まるよう長体処理
- Canvas：scaleX 縮小
- PDF：フォントサイズ縮小

---

## 受験番号欄仕様

| 項目 | 内容 |
|---|---|
| 数字 | 0〜9（10 択） |
| 桁数 | 可変（デフォルト 6 桁） |
| MB | 解答欄と同一パラメータ |
| 枠 | 全体枠 + 桁区切り縦線 + 記入欄 |

---

## 解答欄仕様

| 項目 | 内容 |
|---|---|
| 設問数 | 可変 |
| 選択肢数 | 設問ごとに個別指定 |
| 列数 | 可変（デフォルト 3 列） |
| 枠 | 全体枠 + 設問番号列区切り線 |

### TM 間隔チェック

解答欄の `ansU` 設定値が受験番号欄の最終 TM 終端 + 4.23 mm を下回る場合、自動補正してステータスバーに通知する。

```
補正下限 = 受験番号欄最終TM終端u + 4.23mm
         → mbU に逆算
         → ピッチ境界（pitch の整数倍）に切り上げ
```

---

## PDF 出力仕様

- 用紙：B4（縦型 = 縦長 B4、横型 = 横長 B4）
- 単位：mm（jsPDF unit:'mm'）
- TM：黒塗り矩形（fillColor = 0）
- MB：マゼンタ枠（drawColor = 170, 0, 80）、lineWidth = 0.1 mm
- 外枠：黒線（lineWidth = 0.2 mm）
- スケーリング：なし（jsPDF の mm 単位直接描画）

---

## 参照スクリプト

設計時に参照した Illustrator 用スクリプト：

| スクリプト | 用途 |
|---|---|
| スケールチェック.jsx | MB ピッチ・位置の正誤判定 |
| TMチェック.jsx | TM サイズ・間隔・位置の正誤判定 |
