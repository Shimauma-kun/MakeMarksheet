# マークシート生成ツール v4 仕様書

対象機器: セコニック SR-3500  
出力: B4アートボード上にA4中央配置のPDF

---

## 1. シートサイズ

| | 幅 | 高さ |
|---|---|---|
| 縦型A4 | 210mm | 297mm |
| 横型A4 | 297mm | 210mm |
| 縦型B4 | 257mm | 364mm |
| 横型B4 | 364mm | 257mm |

A4はB4の中央に配置（B4-A4の差の1/2がオフセット）。

---

## 2. 外枠

外枠 = シートサイズ（マージンなし）

---

## 3. ピッチマスター

| ピッチ | pitch(mm) | firstX(mm) | maxMB | tmS(mm) | tmL(mm) | tmPos(mm) |
|---|---|---|---|---|---|---|
| 0.25 | 6.35 | 6.35 | 33 | 0.89 | 5.9 | -2.0 |
| 1/6 | 4.233 | 11.43 | 48 | 1.27 | 3.81 | 5.085 |
| 0.2 | 5.08 | 11.75 | 40 | 0.89 | 5.9 | -2.0 |
| 0.2s | 5.08 | 12.7 | 40 | 0.89 | 3.81 | 5.095 |
| 0.3 | 7.62 | 14.50 | 27 | 0.89 | 7.0 | -2.0 |
| 0.3F | 7.62 | 17.78 | 24 | 0.5 | 5.08 | 7.62 |

- **pitch**: MBの配置間隔（隣り合う２つのMBの中心の距離）
- **firstX**: 外枠端（縦型=左端、横型=下端）から第１MBの中心までの距離
- **maxMB**: 最大MB数
- **tmS**: TM短辺（センサー通過方向の長さ）
- **tmL**: TM長辺（TM固定辺方向の長さ）
- **tmPos**: 外枠端からTM端までのオフセット（負=外枠外側）

---

## 4. タイミングマーク（TM）仕様

### 4.1 制御型TMのみ対応

TM最小間隔: **4.23mm**（TM上端〜次TM上端）

### 4.2 TMとMBの位置関係（制御型）

```
縦型: MB中心y = TM上端y + tmS × 1.5
横型: MB中心x = TM左端x + tmS × 1.5
```

TM短辺 = センサー通過方向の寸法。  
TM上端（縦型）/ TM左端（横型）から tmS × 1.5 進んだ位置にMB中心が来る。

### 4.3 縦型・横型のTM配置

| | 縦型 | 横型 |
|---|---|---|
| TM固定辺 | 左辺（x固定） | 下辺（y固定） |
| TM固定座標（おもて） | x = outer.x1 + tmPos | y = outer.y2 - tmPos - tmH |
| TM変化座標 | y = MB中心y - tmS×1.5 | x = MB中心x - tmS×1.5 |
| TM幅×高さ | tmL × tmS（横長） | tmS × tmL（縦長） |

tmPos < 0 の場合、TMは外枠の外側（塗り足し領域）に配置される。  
0.25ピッチ横型の例: TM上端y = outer.y2 - tmL + |tmPos| = 205 - 5.9 + 2 = 201.1mm（外枠内）、TM下端 = 207mm（外枠外）。

---

## 5. マークボックス（MB）仕様

### 5.1 MBとは

TMを起点として、TMの長辺方向に展開するように配置する。  
MBは必要個数分だけ配置すればよく、最大 maxMB 個まで配置可能。  
主に問題の選択肢として使用され、鉛筆で塗ると赤外線センサーが検知する。

### 5.2 MB寸法

ユーザー入力: 短辺(mbS)・長辺(mbL)、デフォルト mbS=2.5mm, mbL=4mm

| | 縦型 | 横型 |
|---|---|---|
| MB幅（x方向） | mbL（長辺・横長） | mbS（短辺・縦長） |
| MB高さ（y方向） | mbS（短辺・横長） | mbL（長辺・縦長） |

### 5.3 MB垂直方向基準

「縦型の左端 = 横型の下端」の対応関係。

| | 縦型 | 横型 |
|---|---|---|
| おもて | MB中心x = outer.x1 + firstX | MB中心y = outer.y2 - firstX |
| うら | MB中心x = outer.x2 - firstX | MB中心y = outer.y1 + firstX |

根拠（スクリプト解析・下↓正座標系で確認済み）:  
縦型おもて: TM左端x + |tmPos| = outer.x1 → MB中心x = outer.x1 + firstX  
横型おもて: スクリプトを旧座標系→下↓正変換 → MB中心y = outer.y2 - firstX

### 5.4 MB内番号表示

MBの中に選択肢番号（1,2,3...）または受験番号の数字（0〜9）をマゼンタで長体表示。

---

## 6. レイアウト仕様

縦型・横型でレイアウトの向きは共通（コードは単一の calcLayout 関数）。

| | センサー通過方向（along） | 垂直方向（perp） |
|---|---|---|
| 縦型 | y軸（上→下・y増加） | x軸（左→右・x増加） |
| 横型 | x軸（左→右・x増加） | y軸（下→上・y減少）← 下端が起点 |

### 6.1 受験番号欄

| | 縦型 | 横型 |
|---|---|---|
| 0〜9の展開方向 | y方向（下↓）enStart + ri×pitch | x方向（右→）enStart + ri×pitch |
| 桁の展開方向 | x方向（右→）mbPerp + di×pitch | y方向（上↑）mbPerp - di×pitch |
| TM | y変化・x固定 | x変化・y固定 |

enStart = 縦型ではy座標、横型ではx座標（センサー通過方向の開始位置）

### 6.2 解答欄

| | 縦型 | 横型 |
|---|---|---|
| 選択肢の展開方向 | x方向（右→）colPerp + ci×pitch | y方向（上↑）colPerp - ci×pitch |
| 設問の展開方向 | y方向（下↓）ansStart + qi×pitch | x方向（右→）ansStart + qi×pitch |
| 列の折り返し方向 | x方向（右→）colPerp += ... | y方向（上↑）colPerp -= ... |
| TM | y変化・x固定 | x変化・y固定 |

ansStart = 縦型ではy座標、横型ではx座標（センサー通過方向の開始位置）

展開方向の根拠（スクリプト解析）:  
Illustratorの translate(0, +pitch) は旧座標系（上↑がy正）なので画面上では上↑方向への移動。  
横型スクリプトでは dy = +pitch でMBを展開 → 画面上で上↑方向に展開される。

### 6.3 TM重複排除

- 縦型: y座標で重複排除（許容誤差 0.01mm）
- 横型: x座標で重複排除（許容誤差 0.01mm）

---

## 7. 重なり防止

受験番号欄と解答欄が重ならないよう ansStart を自動補正：

1. 枠同士チェック: 受験番号欄枠終端 + BOX_PAD を下回る場合に補正
2. TM間隔チェック: 受験番号欄最終TM終端 + 4.23mm + tmS×1.5 を下回る場合に補正

補正時はステータスバーに警告表示。

---

## 8. おもて・うら面の違い

| | おもて | うら |
|---|---|---|
| TM配置辺 | 縦型=左辺、横型=下辺 | 縦型=右辺、横型=上辺 |
| MB垂直基準 | 縦型=左端+firstX、横型=下端-firstX | 縦型=右端-firstX、横型=上端+firstX |
| MB展開方向（perp） | 縦型=右→、横型=上↑ | 縦型=左←、横型=下↓ |

---

## 9. 座標系まとめ（0.25ピッチの例）

縦型おもて（outer: x1=8, y1=5, x2=202, y2=292）

```
TM x固定    = outer.x1 + tmPos = 8 + (-2) = 6mm（外枠より2mm左）
MB中心x基準 = outer.x1 + firstX = 8 + 6.35 = 14.35mm

受験番号 ri=0: MB中心y = enStart,      TM y上端 = enStart - 1.335
受験番号 ri=1: MB中心y = enStart+6.35, TM y上端 = enStart + 6.35 - 1.335
選択肢 ci=0: MB中心x = 14.35
選択肢 ci=1: MB中心x = 14.35 + 6.35 = 20.70（右→）
```

横型おもて（outer: x1=8, y1=5, x2=289, y2=205）

```
TM y固定    = outer.y2 - tmPos - tmH = 205 - (-2) - 5.9 = 201.1mm
MB中心y基準 = outer.y2 - firstX = 205 - 6.35 = 198.65mm

受験番号 ri=0: MB中心x = enStart,      TM x左端 = enStart - 1.335
受験番号 ri=1: MB中心x = enStart+6.35, TM x左端 = enStart + 6.35 - 1.335
桁 di=0: MB中心y = 198.65
桁 di=1: MB中心y = 198.65 - 6.35 = 192.30（上↑）
選択肢 ci=0: MB中心y = 198.65
選択肢 ci=1: MB中心y = 198.65 - 6.35 = 192.30（上↑）
```

---

## 10. 未実装・今後の課題

- [ ] オーバーフロー検知（外枠からはみ出す場合の警告）
- [ ] うら面のレイアウト検証
- [ ] 列数の自動計算オプション
- [ ] GitHub push
