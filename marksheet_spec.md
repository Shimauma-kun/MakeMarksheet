# マークシート生成ツール 仕様書

**対象機器:** セコニック SR-3500  
**出力:** B4アートボード上にA4中央配置のPDF  
**対応向き:** 縦型A4 / 横型A4  
**対応ピッチ:** 0.25 / 1/6 / 0.2 / 0.2s / 0.3 / 0.3F

---

## 1. 用語定義

| 用語 | 略称 | 説明 |
|---|---|---|
| タイミングマーク | TM | スキャナーがMBの位置を検出するための黒塗り矩形。外枠の辺に沿って等間隔に並ぶ。 |
| マークボックス | MB | 受験者が鉛筆でマークする楕円形の枠。各TMに対応して配置される。 |
| ピッチ | pitch | TM中心間・MB中心間の間隔（mm）。 |
| along方向 | along | TMが並ぶ方向。縦型＝下↓（y増加）、横型＝右→（x増加）。 |
| perp方向 | perp | along方向と直交する方向。縦型＝右→（x増加）、横型＝上↑（y減少）。 |
| 外枠 | outer | A4サイズの印刷領域枠（マージンなし）。座標計算の基準。 |
| 物理MB番号 | - | スキャナー基準のMB番号。TM配置辺（基準辺）に最も近いMBが第1MB。 |
| 論理問題番号 | - | 受験者が読む問題の順番。シートの上側または左側から問1となる。 |

---

## 2. 座標系

- 原点はA4外枠の**左上隅**（outer.x1=0, outer.y1=0）
- **Web座標系**を採用：右方向がx増加、**下方向がy増加**
- 「上↑」はy減少方向を意味する

---

## 3. 用紙・アートボードサイズ

| 種別 | 幅(mm) | 高さ(mm) |
|---|---|---|
| 縦型A4（外枠） | 210 | 297 |
| 横型A4（外枠） | 297 | 210 |
| 縦型B4（アートボード） | 257 | 364 |
| 横型B4（アートボード） | 364 | 257 |

A4外枠はB4アートボードの中央に配置する。オフセット＝(B4－A4)÷2。

| | x方向オフセット(mm) | y方向オフセット(mm) |
|---|---|---|
| 縦型 | (257－210)÷2 ＝ 23.5 | (364－297)÷2 ＝ 33.5 |
| 横型 | (364－297)÷2 ＝ 33.5 | (257－210)÷2 ＝ 23.5 |

---

## 4. ピッチマスター

スキャナーの読み取り仕様に基づく固定パラメータ。ユーザーは変更不可。

| ピッチ名 | pitch(mm) | firstX(mm) | maxMB | tmS(mm) | tmL(mm) | tmPos(mm) |
|---|---|---|---|---|---|---|
| 0.25 | 6.35 | 6.35 | 33 | 0.89 | 5.9 | -2.0 |
| 1/6 | 4.233 | 11.43 | 48 | 1.27 | 3.81 | 5.085 |
| 0.2 | 5.08 | 11.75 | 40 | 0.89 | 5.9 | -2.0 |
| 0.2s | 5.08 | 12.7 | 40 | 0.89 | 3.81 | 5.095 |
| 0.3 | 7.62 | 14.50 | 27 | 0.89 | 7.0 | -2.0 |
| 0.3F | 7.62 | 17.78 | 24 | 0.5 | 5.08 | 7.62 |

**パラメータの意味:**

- **pitch**: TM中心間・MB中心間の間隔
- **firstX**: 外枠基準辺からMB中心第1番目までの距離（縦型=左辺、横型=下辺）
- **tmS**: TMのalong方向の寸法（短辺）
- **tmL**: TMのperp方向の寸法（長辺）
- **tmPos**: 外枠基準辺からTM端までのオフセット。**負値=外枠の外側**にはみ出す
- **maxMB**: along方向に配置できるMBの最大数

---

## 5. TM・MBのサイズと形状

### 5.1 TM（タイミングマーク）

黒塗りの矩形。向きによって縦長・横長が変わる。

| | 縦型 | 横型 |
|---|---|---|
| along方向の寸法 | tmS（短辺） | tmS（短辺） |
| perp方向の寸法 | tmL（長辺） | tmL（長辺） |
| 形状 | 横長の矩形 | 縦長の矩形 |
| 配置辺 | 左辺（外枠外） | 下辺（外枠外） |

### 5.2 MB（マークボックス）

標準サイズ: **長辺4mm × 短辺2mm**。塗りなし・マゼンタ線の楕円形。

| | 縦型 | 横型 |
|---|---|---|
| along方向の寸法（mbS） | 2mm（短辺） | 2mm（短辺） |
| perp方向の寸法（mbL） | 4mm（長辺） | 4mm（長辺） |
| 形状 | 横長の楕円 | 縦長の楕円 |

---

## 6. TMとMBの位置関係（SR-3500仕様・変更不可）

スキャナーはTMを検出した後、along方向に `tmS × 1.5` 進んだ位置でMBを読み取る。この値はSR-3500の機器仕様であり変更不可。

```
縦型: MB中心y = TM上端y + tmS × 1.5    （along=下↓方向にずれる）
横型: MB中心x = TM左端x + tmS × 1.5    （along=右→方向にずれる）
```

**MB中心の固定座標（perp方向の起点）:**

```
縦型: MB先頭のx座標（固定） = outer.x1 + firstX
横型: MB先頭のy座標（固定） = outer.y2 - firstX
```

TMの最小間隔（along方向、上端から次の上端まで）: **4.23mm**

---

## 7. 物理MB番号と論理番号の方向

### 7.1 縦型

```
outer.x1=0（左辺・TM配置辺）
│
TM─── 第1MB 第2MB 第3MB 第4MB 第5MB ───→ perp（右→）
│     選択肢1 選択肢2 選択肢3 選択肢4 選択肢5
TM───  （問2）
│
↓ along（下↓）＝TM番号増加方向＝問題番号増加方向
```

**縦型のポイント（逆転なし）:**

| 要素 | 方向 | 備考 |
|---|---|---|
| TM番号 | along（下↓） | 第1TM→第2TM→第3TM... |
| 問題番号 | along（下↓） | 問1→問2→問3... TMと同方向 |
| MB番号 | perp（右→） | 第1MB→第2MB→第3MB... |
| 選択肢番号 | perp（右→） | 選択肢1→2→3... MBと同方向 |

縦型では**すべての番号が同方向で逆転が発生しない**。

### 7.2 横型

```
outer.y1=0（上辺）
↑
第4MB（問1）─ 選択肢1 選択肢2 選択肢3 ──→ along（右→）
第3MB（問2）─ 選択肢1 選択肢2 選択肢3
第2MB（問3）─ 選択肢1 選択肢2 選択肢3
第1MB（問4）─ 選択肢1 選択肢2 選択肢3
↓
outer.y2=210mm（下辺・TM配置辺）
TM    TM    TM    ──→ along（右→）
```

**横型のポイント（問題番号とMB番号が逆方向）:**

| 要素 | 方向 | 備考 |
|---|---|---|
| TM番号 | along（右→） | 第1TM→第2TM→第3TM... |
| 選択肢番号 | along（右→） | 選択肢1→2→3... TMと同方向 |
| MB番号 | perp（上↑、y減少） | 第1MB=下辺最寄り、第nMBは上へ |
| 問題番号 | perp（下↓、y増加） | 問1=上側、問2→3...は下へ |

横型では**MB番号の増加方向（上↑）と問題番号の増加方向（下↓）が逆転する**。

**座標の対応:**

```
第nMBのy = outer.y2 - firstX - (n-1) × pitch   （上↑、y減少）
問mのy   = ansStartY + (m-1) × pitch             （下↓、y増加）

ansStartY = outer.y2 - firstX - (startMB-1) × pitch
（startMB: 問1を第何MBの位置から開始するか）
```

---

## 8. 解答欄レイアウト

### 8.1 座標計算

**縦型:**

```
問題番号qi（along=下↓）:
  MB中心y = ansStart + qi × pitch

選択肢ci（perp=右→）:
  MB中心x = colBaseX + ci × pitch

列折返し: colBaseXをperp（右→）方向に増加
```

**横型:**

```
選択肢ci（along=右→）:
  MB中心x = ansStart + ci × pitch

問題番号qi（perp=下↓、y増加）:
  MB中心y = ansStartY + qi × pitch

列折返し: ansStartをalong（右→）方向に増加
```

### 8.2 ユーザー設定

| パラメータ | 初期値 | 説明 |
|---|---|---|
| questionCount | 50 | 問題数 |
| choiceCount | 5 | 選択肢数 |
| startMB | 24 | 問1を第何MBの位置から開始するか |
| columnGap | pitch × 2 | 列折返し時の間隔 |
| sectionGap | pitch × 2 | 受験番号欄と解答欄の間隔 |

---

## 9. 受験番号欄レイアウト

### 9.1 縦型

```
数字0〜9（along=下↓）:
  MB中心y = enStart + ri × pitch    （ri=0〜9、0が上・9が下）

桁（perp=右→）:
  MB中心x = outer.x1 + firstX + di × pitch    （di=0〜examDigits-1）
```

### 9.2 横型

```
桁（along=右→）:
  MB中心x = enStart + di × pitch    （di=0〜examDigits-1）

数字0〜9（perp=下↓）:
  MB中心y = examTopY + ri × pitch   （ri=0〜9、0が上・9が下）

examTopY = ansStartY（解答欄の問1と同じy座標に上端を合わせる）
```

### 9.3 ユーザー設定

| パラメータ | 初期値 | 説明 |
|---|---|---|
| examDigits | 5 | 受験番号の桁数 |
| enStart | firstX + pitch × 3 | 受験番号欄の先頭MB位置（along方向） |

> **enStartについて:** SR-3500では第1マーク（index=0）は通常使用しない慣習がある。また第2・第3マーク付近は印字領域として確保することを考慮し、デフォルトは第4マーク（index=3）とする。

---

## 10. テキスト要素

### 10.1 MB内の数値（選択肢番号・受験番号の数字）

各MBの内部中央に数値を印字する。

| 項目 | 値 |
|---|---|
| 色 | マゼンタ（ドロップアウトカラー） |
| サイズ | MBの短辺に合わせた最大サイズ |
| 目的 | スキャナーにはドロップアウトされ、受験者の視認のみに使用 |

### 10.2 問題番号

各問の選択肢1のMB左側に横書きで表示。

| 項目 | 値 |
|---|---|
| 色 | マゼンタ |
| サイズ | MB内テキストと同等 |
| 位置 | 選択肢1のMB左端から左側 |

### 10.3 桁ラベル（百位・十位…）

**不要。** MBのみ描画する。

---

## 11. 枠の描画

受験番号欄・解答欄それぞれにマゼンタの実線矩形を描画する。

| 項目 | 値 |
|---|---|
| 色 | マゼンタ |
| 線種 | 実線 |
| パディング | 2mm（MBの端から枠線まで） |

---

## 12. セクション配置

along方向の先頭から「受験番号欄 → 解答欄」の順に固定。

**重なり防止チェック:**

```
縦型: 受験番号欄終端y < 解答欄開始y
横型: 受験番号欄終端x < 解答欄開始x
```

チェックが失敗した場合はユーザーに警告を表示し、PDF生成を行わない。

---

## 13. TM重複排除

受験番号欄と解答欄の境界付近でTMが重なる場合、along方向の座標が同じTMは1個のみ描画する。

---

## 14. ユーザー入力パラメータ一覧

| パラメータ | 初期値 | 説明 |
|---|---|---|
| orientation | 縦型 | シートの向き（縦型 / 横型） |
| pitch | 0.25 | ピッチ種別（ピッチマスター参照） |
| examDigits | 5 | 受験番号の桁数 |
| questionCount | 50 | 問題数 |
| choiceCount | 5 | 選択肢数 |
| startMB | 24 | 問1（解答欄）・数字0（受験番号欄）の開始MB番号 |
| enStart | firstX + pitch × 3 | 受験番号欄の先頭MB位置（along方向） |
| sectionGap | pitch × 2 | 受験番号欄〜解答欄の間隔（pitch倍数で指定） |
| columnGap | pitch × 2 | 解答欄の列折返し間隔（pitch倍数で指定） |

---

## 15. 横型シートのMB番号と問題番号の方向（重要）

横型シートでは「物理的なMB番号」と「論理的な問題番号」が**逆方向**に展開する。

### 物理MB番号（スキャナー基準）

TMは外枠の**下辺**に配置される。下辺に最も近いMBが第1MBで、上方向（y減少）に向かって第2MB、第3MB...と増加する。

```
outer.y1=0（上辺）
↑
第nMB   ← outer.y2 - firstX - (n-1)×pitch
...
第2MB
第1MB   ← outer.y2 - firstX（下辺からfirstX上）
outer.y2=210mm（下辺）
TM（外枠外）
```

### 論理問題番号（受験者基準）

受験者がシートを見たとき、上側から問1となり、下方向（y増加）に向かって問2、問3...と展開する。

```
outer.y1=0（上辺）
↓
問1（ansStartY）
問2（ansStartY + pitch）
問3（ansStartY + 2×pitch）
...
outer.y2=210mm（下辺）
```

### 両者の対応

問1を第startMBから開始する場合:

```
ansStartY = outer.y2 - firstX - (startMB-1) × pitch

問mのy = ansStartY + (m-1) × pitch  （下↓、y増加）
第nMBのy = outer.y2 - firstX - (n-1) × pitch  （上↑、y減少）
```

### 縦型との比較

| | 縦型 | 横型 |
|---|---|---|
| TM・選択肢方向 | along（同方向） | along（同方向） |
| MB番号方向 | perp（右→） | perp（上↑） |
| 問題番号方向 | along（下↓）＝TM同方向 | perp（下↓）＝MB番号と逆方向 |
| 逆転 | **なし** | **MB番号と問題番号が逆転** |

---

## 16. 座標計算例（ピッチ 0.25）

### 縦型（outer: x1=0, y1=0, x2=210, y2=297）

```
pitch=6.35mm、firstX=6.35mm、tmS=0.89mm、tmL=5.9mm、tmPos=-2.0mm

TM左端x（固定） = 0 + (-2.0) = -2.0mm  ※外枠左辺より2mm外側
MB先頭x（固定） = 0 + 6.35 = 6.35mm
enStart         = 6.35 + 6.35×3 = 25.4mm

受験番号欄:
  桁di=0: MB中心x = 6.35mm
  桁di=1: MB中心x = 12.70mm
  数字ri=0: MB中心y = 25.4mm（上）
  数字ri=9: MB中心y = 25.4 + 9×6.35 = 82.55mm（下）
```

### 横型（outer: x1=0, y1=0, x2=297, y2=210）

```
pitch=6.35mm、firstX=6.35mm、tmS=0.89mm、tmL=5.9mm、tmPos=-2.0mm、startMB=24

TM上端y（固定） = 210 - (-2.0) - 5.9 = 206.1mm  ※外枠下辺より2mm外側
第1MB中心y     = 210 - 6.35 = 203.65mm（下辺最寄り）
ansStartY      = 210 - 6.35 - 23×6.35 = 57.60mm（問1のy、第24MB）

解答欄:
  問1: MB中心y = 57.60mm（上側）
  問2: MB中心y = 63.95mm（下↓）
  選択肢1: MB中心x = ansStart（along=右→）
  選択肢2: MB中心x = ansStart + 6.35mm
```

---

## 17. 未実装・今後の課題

- [ ] PDF出力機能
- [ ] オーバーフロー検知（外枠からはみ出す場合の警告）
- [ ] うら面のレイアウト検証
- [ ] GitHub push
