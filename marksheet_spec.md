# マークシート生成ツール v4 仕様書

対象機器: セコニック SR-3500  
出力: B4アートボード上にA4中央配置のPDF

---

## 1. シートサイズ

| | 幅 | 高さ |
|---|---|---|
| 縦型A4 | 210mm | 297mm |
| 横型A4 | 297mm | 210mm |
| 縦型B4 | 257mm | 364mm |
| 横型B4 | 364mm | 257mm |

A4はB4の中央に配置（オフセット = (B4-A4)/2）。  
外枠 = A4シートサイズ（マージンなし）。

---

## 2. ピッチマスター

| ピッチ | pitch(mm) | firstX(mm) | maxMB | tmS(mm) | tmL(mm) | tmPos(mm) |
|---|---|---|---|---|---|---|
| 0.25 | 6.35 | 6.35 | 33 | 0.89 | 5.9 | -2.0 |
| 1/6 | 4.233 | 11.43 | 48 | 1.27 | 3.81 | 5.085 |
| 0.2 | 5.08 | 11.75 | 40 | 0.89 | 5.9 | -2.0 |
| 0.2s | 5.08 | 12.7 | 40 | 0.89 | 3.81 | 5.095 |
| 0.3 | 7.62 | 14.50 | 27 | 0.89 | 7.0 | -2.0 |
| 0.3F | 7.62 | 17.78 | 24 | 0.5 | 5.08 | 7.62 |

- **pitch**: MB中心間隔
- **firstX**: 外枠端（縦型=左端、横型=下端）からMB中心1番目までの距離
- **tmS**: TM短辺（センサー通過方向）
- **tmL**: TM長辺（TM固定辺方向）
- **tmPos**: 外枠端からTM端までのオフセット（負=外枠外側）

---

## 3. TM・MB物理仕様

### 3.1 縦型

```
TM配置辺: 左辺
TM固定x（左端）= outer.x1 + tmPos        （tmPos<0なら外枠外）
TM変化y（上端）= MB中心y - tmS×1.5
TM幅×高さ: tmL × tmS（横長）

MB中心x（固定）= outer.x1 + firstX
MB中心y（変化）= along方向（下↓）
MB幅×高さ: mbL × mbS（横長）
```

### 3.2 横型

```
TM配置辺: 下辺
TM固定y（上端）= outer.y2 - tmPos - tmH  （tmPos<0なら外枠外）
TM変化x（左端）= MB中心x - tmS×1.5
TM幅×高さ: tmS × tmL（縦長）

MB中心y（固定）= outer.y2 - firstX
MB中心x（変化）= 各MB列のx座標（perp方向）
MB幅×高さ: mbS × mbL（縦長）
```

### 3.3 TM・MB位置関係（制御型）

```
縦型: MB中心y = TM上端y + tmS×1.5
横型: MB中心x = TM左端x + tmS×1.5
```

TM最小間隔: **4.23mm**（上端〜次の上端）

---

## 4. レイアウト仕様

### 4.1 縦型・横型の対応関係

```
         縦型                    横型
─────────────────────────────────────────────
TM並び方向    y方向（下↓）         x方向（右→）
MB並び方向    x方向（右→）         y方向（上↑）
along軸       y（下↓）             x（右→）
perp軸        x（右→）             y（上↑=y減少）
MB中心固定値  x = outer.x1+firstX   y = outer.y2-firstX
─────────────────────────────────────────────
```

**Webアプリ座標系（下↓がy増加）での「上↑」はy減少方向。**  
Illustratorスクリプトで `translate(0, +pitch)` が画面上↑なのと同じ理由。

### 4.2 受験番号欄

| | 縦型 | 横型 |
|---|---|---|
| 数字0〜9の展開 | along=y（下↓）: `enStart + ri×pitch` | along=x（右→）: `enStart + ri×pitch` |
| 桁の展開 | perp=x（右→）: `mbBaseX + di×pitch` | perp=y（上↑）: `mbPerpBase - di×pitch` |
| TM | y変化・x固定 | x変化・y固定 |
| 枠終端 | y方向（examEnd=y座標） | x方向（examEnd=x座標） |

- 縦型 `enStart`: y座標（受験番号欄の数字0の中心y）
- 横型 `enStart`: x座標（受験番号欄の数字0の中心x）
- 縦型 `mbBaseX` = `outer.x1 + firstX`
- 横型 `mbPerpBase` = `outer.y2 - firstX`（1番目MB中心y）

### 4.3 解答欄

| | 縦型 | 横型 |
|---|---|---|
| 設問の展開 | along=y（下↓）: `ansStart + qi×pitch` | along=x（右→）: `ansStart + qi×pitch` |
| 選択肢の展開 | perp=x（右→）: `colPerpX + ci×pitch` | perp=y（上↑）: `colPerpY - ci×pitch` |
| 列折返し | perp=x（右→）増加 | perp=y（上↑）減少 |
| TM | y変化・x固定 | x変化・y固定 |

- 縦型 `ansStart`: y座標（解答欄の設問1の中心y）
- 横型 `ansStart`: x座標（解答欄の設問1の中心x）

### 4.4 TM重複排除

- 縦型: y座標で重複排除
- 横型: x座標で重複排除

---

## 5. 座標計算例（0.25ピッチ）

### 縦型（outer: x1=0, y1=0, x2=210, y2=297）

```
TM固定x左端 = 0 + (-2) = -2mm（外枠外）
MB中心x固定 = 0 + 6.35 = 6.35mm

受験番号 ri=0: MB中心y = enStart
               TM上端y = enStart - 1.335mm
受験番号 ri=1: MB中心y = enStart + 6.35mm
桁 di=0: MB中心x = 6.35mm
桁 di=1: MB中心x = 12.70mm
```

### 横型（outer: x1=0, y1=0, x2=297, y2=210）

```
TM固定y上端 = 210 - (-2) - 5.9 = 206.1mm（外枠内3.9mm、外枠外2mm）
MB中心y固定 = 210 - 6.35 = 203.65mm（外枠下端から6.35mm上）

受験番号 ri=0: MB中心x = enStart
               TM左端x = enStart - 1.335mm
受験番号 ri=1: MB中心x = enStart + 6.35mm
桁 di=0: MB中心y = 203.65mm
桁 di=1: MB中心y = 197.30mm（上↑方向）
```

---

## 6. 重なり防止

- 縦型: y方向で受験番号欄終端 < 解答欄開始をチェック
- 横型: x方向で受験番号欄終端 < 解答欄開始をチェック

---

## 7. 未実装・今後の課題

- [ ] オーバーフロー検知（外枠からはみ出す場合の警告）
- [ ] うら面のレイアウト検証
- [ ] GitHub push
